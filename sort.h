#ifndef SORT_H
#define SORT_H
#include <stddef.h>
#include <unistd.h>

void print_int(int n);
int _putchar(char c);
void bubble_sort(int *array, size_t size);
void print_array(int *array, int size);

#endif
